This repository contains a Shiny web application built in R that provides an interactive environment for data preprocessing, cleaning, and machine learning (ML) modeling.
The app is designed to guide users through the entire data analysis pipeline, from raw data to model selection.
